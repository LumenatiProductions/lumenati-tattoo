import "../s/s.css";

// The add-your-shop wizard shares the standard template's scoped Tailwind.
export default function StartLayout({ children }: { children: React.ReactNode }) {
  return children;
}
